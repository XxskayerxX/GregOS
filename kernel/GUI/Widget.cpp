/* GUI::Widget — abstract base. All virtual dispatch lives in subclasses.
   This translation unit exists so the linker sees the vtable.            */

#include "../../include/GUI/Widget.hpp"
#include "../../include/Graphics.hpp"
